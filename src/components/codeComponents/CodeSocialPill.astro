{`export const SOCIALS = [
            {
                href: 'https://www.linkedin.com/in/franco-alvarez-8ab600262/',
                icon: 'LinkedInIcon',
                title:'LinkedIn'
            },
            {
                href: 'https://github.com/FrancoAlvarez83/',
                icon: 'GitHubIcon',
                title:'GitHub'
            }
            ...
        ]        
`}

<p>
    <pre>
      <code>
        &lt;nav class="flex flex-wrap gap-4 mt-3"&gt;
            {`SOCIALS.map(({ href, icon, title }) => { 
                return (
                    <SocialPill key={title} href={href} class="rounded-lg border border-white/10 flex justify-center items-center gap-x-2 py-1 px-2 md:py-2 md:px-4 text-xs md:text-base bg-white/5 hover:scale-105 hover:bg-white/10 transition"                     
                        target="_blank"
                        rel="noopener noreferrer">                    
                            {icon}
                            {title}                             
                    </SocialPill>
                );
             })
        `}&lt;/nav&gt;
      </code>
    </pre>
  </p>
  